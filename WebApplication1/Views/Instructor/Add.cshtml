@using WebApplication1.ViewModel
@model InstructotwithlistDepartmentWithlistCourseViewModel
@{
    ViewData["Title"] = "Add";
}

<h1>Add</h1>

<form   method="post" action="/Instructor/saveAdd/@Model.Id">
   @*  <div>
        <label for="Id">Id : </label>
        <input  type="hidden" n name="Id" id="Id" value="@Model?.Id" class="form form-control" />
    </div> *@

    <div>
        <label for="Name">Name : </label>
        <input type="text" name="Name" id="Name" value="@Model?.Name" class="form form-control" />
    </div> 
    
    <div>
        <label for="imageUrl">imageUrl : </label>
        <input type="text" name="imageUrl" id="imageUrl" value="@Model?.imageUrl" class="form form-control" />
    </div>  
    
    <div>
        <label for="address">address : </label>
        <input type="text" name="address" id="address" value="@Model?.address" class="form form-control" />
    </div>  
  @*   
    <div>
        <label for="crs_id"></label>
        <input type="text" name="crs_id" id="crs_id" value="@Model?.crs_id" class="form form-control" />
    </div> 
     *@
    
  @*   <div>
        <label for="deptId"></label>
        <input type="text" name="deptId" id="deptId" value="@Model?.deptId" class="form form-control" />
    </div>  *@
    
    <div>
        <label for="salary"> salary : </label>
        <input type="text" name="salary" id="salary" value="@Model.salary" class="form form-control" />
    </div>


    <div>
        <label for="deptId">departments </label>
        <select id="deptId" name="deptId" class="form form-control>
            @foreach(Department deptItem in Model.Departments){
                if(Model.deptId==deptItem.Id){

                    <option  value="@deptItem.Id"   selected>@deptItem.Name</option>
                }

                else {
                    <option value="@deptItem.Id">@deptItem.Name</option>
                }
            }
        </select>

    </div>

    <br />

    <div>

        <label for="crs_id">courses </label>
        <select id="crs_id" name="crs_id" class="form form-control">
            @foreach (Course corsItem in Model.Courses)
            {
                if (Model.crs_id == corsItem.Id)
                {

                    <option value="@corsItem.Id" selected>@corsItem.Name</option>
                }

                else
                {
                    <option value="@corsItem.Id">@corsItem.Name</option>
                }
            }
        </select>

    </div>

    <input type="submit" value="Save" class="btn btn-success" />

</form>
<br />
<a href="/Instructor/All">Back To List</a>